using System;
using System.Linq;
using System.Collections.Generic;
using UnityEngine;

public static class ServerHandlers {
    // public static void UpdateReceived(uint _fromClient, Packet _packet) {
    //     int tick = _packet.ReadInt();
    //     Debug.Log("CLIENT OFFSET BY " + (tick - Master.Instance.tick));
    // }
}
